import main_bg from "../data/main_bg.png";

const myBackgroundImage = {
    backgroundImage: `url("${main_bg}")`,
    backgroundSize: 'cover',
    backgroundPosition: 'center',
};

export default myBackgroundImage;